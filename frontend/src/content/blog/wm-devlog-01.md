---
title: '自作 WM 開発記 #1: ウィンドウが出るまで'
description: 'Rust で自作ウィンドウマネージャを作り始めた。最初のウィンドウが表示されるまでの記録。'
pubDate: '2026-08-18'
---

自作ウィンドウマネージャの開発記録その 1。この記事はプレースホルダで、あとで実際の開発の話に差し替える。

## きっかけ

ここにきっかけを書く。ダミーテキスト。タイル型 WM を使っているうちに仕組みが気になり、自分で作れば一番理解が深まるのでは、と思った話などをそれっぽく書く。

## X11 のイベントループ

ここに技術的な内容を書く。ダミーテキスト。ディスプレイサーバへの接続、イベントの受信、`MapRequest` をハンドリングしてウィンドウを表示するあたりの流れを書く予定。

```rust
// placeholder: あとで実際のコードに差し替える
loop {
    let event = connection.wait_for_event()?;
    match event {
        Event::MapRequest(e) => manager.map_window(e.window)?,
        Event::ConfigureRequest(e) => manager.configure(e)?,
        _ => {}
    }
}
```

## つまずいたところ

- ここにハマりポイントを書く。ダミーテキスト
- エラー処理まわりで悩んだ話
- ドキュメントよりも既存 WM のソースを読むほうが早かった話

## 次にやること

タイリングのレイアウトアルゴリズムを実装する。次回に続く。
